# Listen for client connections on all network interfaces
port: 4222

# HTTP monitoring port
http_port: 8222

# Enable JetStream persistence
jetstream {
  store_dir: /data/nats
  max_mem: 1G
  max_file: 10G
}

# --- Stream Definitions ---
# The HELM platform requires durable streams for certain topics to ensure
# messages are not lost, even if a consuming service is temporarily offline.

# Durable stream for all audit events

# Durable stream for command approval workflows
